RewriteEngine On
RewriteBase /

# Keep real files/directories accessible
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all application URLs to index.php
RewriteRule ^ index.php [L,QSA]
